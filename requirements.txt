# snail-shell 项目依赖

# ── 核心机器学习（树模型）────────────────────────────────────────
lightgbm>=3.3.5
xgboost>=2.0.0          # XGBoost 后端（reg:quantileerror 需 2.0+）
catboost>=1.2.0         # CatBoost 后端

# ── 神经网络 ─────────────────────────────────────────────────────
# CPU-only 安装（无 GPU）：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# GPU 安装（Kaggle / 有显卡）：
#   pip install torch
torch>=2.0.0

# ── 数据处理 ─────────────────────────────────────────────────────
pandas>=1.5.0
numpy>=1.21.0
duckdb>=0.9.0

# ── 机器学习工具 ─────────────────────────────────────────────────
scikit-learn>=1.0.0

# ── 统计与科学计算 ────────────────────────────────────────────────
scipy>=1.9.0
statsmodels>=0.13.0

# ── 可视化 ───────────────────────────────────────────────────────
matplotlib>=3.5.0
seaborn>=0.11.0

# ── 数据源 ───────────────────────────────────────────────────────
akshare>=1.12.0

# ── 工具库 ───────────────────────────────────────────────────────
tqdm>=4.64.0
python-dateutil>=2.8.0
pytz>=2022.7

# ── 开发工具（可选）─────────────────────────────────────────────
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
jupyter>=1.0.0
notebook>=6.4.0
